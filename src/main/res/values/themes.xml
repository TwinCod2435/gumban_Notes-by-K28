<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.NotesByK28" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorSurface">@color/cardview_light_background</item>
        <item name="colorPrimary">@color/light_yellow</item>
        <item name="colorOnSurface">@color/black</item>

        <item name="android:windowBackground">?attr/colorSurface</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.NotesByK28" parent="Base.Theme.NotesByK28" />
</resources>